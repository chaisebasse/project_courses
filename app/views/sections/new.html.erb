<h1>Sections#new</h1>
<%= simple_form_for @section do |f| %>
  <%= f.input :title,
        label: "section title :" %>
  <%= f.input :course_id, collection: Course.all, label: "Course", label_method: :title %>

  <%= f.button :submit, "Create Section" %>
<% end %>
