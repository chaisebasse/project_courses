<h1>Courses#new</h1>

<div class="container">
  <%= simple_form_for @course do |c| %>
    <div style="margin-bottom: 40px">
      <h4>Enter Course Title</h4>
      <%= c.input :title, class: "form" %>
      <%= c.input :price_cents, class: "form" %>
      <%= c.input :video,
            required: true,
            as: :file,
            class: "form" %>
    </div>

    <div class="mt-3"><%= c.button :submit, class: "form" %></div>
  <% end %>
</div>
